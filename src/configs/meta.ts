import { MetaData } from "../type/app";
import { Const } from "./const";

export const META_DATA: MetaData = {
    version: Const.APP_VERSION,
    config: {
        fullscreen: true,
        mute: false
    },
    slots: []
};